/* @flow */
export type ApiUser = {
  avatar_url: string,
  bot_type?: number,
  bot_owner?: string,
  email: string,
  full_name: string,
  is_admin: boolean,
  is_active: boolean,
  is_bot: boolean,
  profile_data?: Object,
  timezone: string,
  user_id: 5584,
};
